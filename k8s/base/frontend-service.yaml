apiVersion: v1
kind: Service
metadata:
  name: tabor-flow-web
  namespace: tabor-flow
spec:
  type: ClusterIP
  selector:
    app: tabor-flow-web
  ports:
    - name: http
      port: 80
      targetPort: http
